import styles from './TabsPanel.module.scss';

export function Progress() {
  return (
    <div>
      <ul>
        <li>Progress list goes here</li>
        <li>Let's see what's needed or possible first with the new 'middleware'</li>
      </ul>
    </div>
  );
}
